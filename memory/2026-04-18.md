- HidenCloud 自动续期方向已从 VNC/GUI/OCR 点击方案切换为接口化主方案。已在工作区建立本地版目录 `/root/.openclaw/workspace/hidencloud-renew-local`，基于 `cwyman12/hidencloud_US` 改造，确认可在本机用 `requests + cookie` 登录并识别服务。
- 用户的目标服务固定为 `service_id=207229`，续期规则已确认：仅在到期前不足 1 天时允许 `Renew`，成功后 `Due date` 增加 7 天。
- 已从服务器上的 Epiphany 登录态中找到并导出 HidenCloud cookie：源数据库在 `/root/.local/share/epiphany/cookies.sqlite`，本地导出配置为 `/root/.openclaw/workspace/hidencloud-renew-local/.env.local`。后续方向是自动从 Epiphany 刷新 cookie。
- 已在本地版中预留多账号配置，模板文件为 `/root/.openclaw/workspace/hidencloud-renew-local/.env.accounts.example`，预期支持 `HIDEN_COOKIE`, `HIDEN_COOKIE_2`, `HIDEN_COOKIE_3` 等。
- 已建立定时检查框架：`/root/.openclaw/workspace/hidencloud-renew-local/run_scheduled_check.py` 与 systemd `hidencloud-renew-check.service` / `hidencloud-renew-check.timer`，计划每天 19:00 检查一次并输出日志到 `logs/scheduled-check.log`，摘要到 `logs/latest-summary.txt`。
- 当前已把 `TARGET_SERVICE_IDS=207229` 加入本地配置，目标是只处理该服务；用户随后明确要求“停”，因此后续未继续推进“清旧 cookie 缓存”等收尾改动。
- 本轮过程中，VNC/noVNC 与 GUI 演练路径证明不适合作为长期主方案；若后续继续，应从 `hidencloud-renew-local` 这条接口化路线接着做，而不是回到 Epiphany + OCR + 坐标点击。
- HidenCloud 自动续期方向已从 VNC/GUI/OCR 点击方案切换为接口化主方案。已在工作区建立本地版目录 `/root/.openclaw/workspace/hidencloud-renew-local`，基于 `cwyman12/hidencloud_US` 改造，确认可在本机用 `requests + cookie` 登录并识别服务。
- 用户的目标服务固定为 `service_id=207229`，续期规则已确认：仅在到期前不足 1 天时允许 `Renew`，成功后 `Due date` 增加 7 天。
- 已从服务器上的 Epiphany 登录态中找到并导出 HidenCloud cookie：源数据库在 `/root/.local/share/epiphany/cookies.sqlite`，本地导出配置为 `/root/.openclaw/workspace/hidencloud-renew-local/.env.local`。后续方向是自动从 Epiphany 刷新 cookie。
- 已在本地版中预留多账号配置，模板文件为 `/root/.openclaw/workspace/hidencloud-renew-local/.env.accounts.example`，预期支持 `HIDEN_COOKIE`, `HIDEN_COOKIE_2`, `HIDEN_COOKIE_3` 等。
- 已建立定时检查框架：`/root/.openclaw/workspace/hidencloud-renew-local/run_scheduled_check.py` 与 systemd `hidencloud-renew-check.service` / `hidencloud-renew-check.timer`，计划每天 19:00 检查一次并输出日志到 `logs/scheduled-check.log`，摘要到 `logs/latest-summary.txt`。
- 当前已把 `TARGET_SERVICE_IDS=207229` 加入本地配置，目标是只处理该服务；用户随后明确要求“停”，因此后续未继续推进“清旧 cookie 缓存”等收尾改动。
- 本轮过程中，VNC/noVNC 与 GUI 演练路径证明不适合作为长期主方案；若后续继续，应从 `hidencloud-renew-local` 这条接口化路线接着做，而不是回到 Epiphany + OCR + 坐标点击。
- 用户明确更正默认称呼：以后应称呼其为“大王”。
- 已为独立 Telegram 小弟 bot 建立隔离工作区方案：brain=`/root/work/opencode-bot/brain`，默认 opencode cwd=`/root/work/opencode-bot/repo`；其定位不是亲自写完所有代码，而是偏“调度 opencode AI 干活、验收结果、推进任务”的执行负责人。
- 小弟 bot 的 brain 文档已初始化并单独维护；已补充规则：收到 GitHub 链接时先识别类型（repo/issue/PR/commit/file/compare/actions/release/gist）、先做基础分析、必要时只追问一个关键问题，再整理成适合 opencode AI 执行的任务，并对回包做验收而不是直接转发“完成了”。
- HidenCloud 本地版当前按“保持现状”执行：Cookie 主要依赖本地缓存（`.env.local` + `hiden_cookies_US.json`），不额外配置 WebDAV 与 WxPusher；结果通知依赖本地 `logs/latest-summary.txt` 由主 bot 读取后转告用户。
- US AWS 上当前 cpap/CLIProxyAPI 主服务端口是 `8317`，OpenClaw 中 `cpap` baseUrl 指向 `http://44.255.146.88:8317/v1`；`8056` 不是当前机器实际在用的 cpap 端口。
- `qwen-auto-register` 仓库中的 `8056` 是其示例里的远程 CLI Proxy API“管理 API”端口，不是 OAuth callback 端口；该项目实际调用的是 `/v0/management/...` 一类管理接口，而非 `1455/54545/8085` 这类浏览器授权回调端口。
- US AWS 上 `cliproxyapi` 当前额外暴露端口的语义：`8085`=Gemini 认证回调，`1455`=Codex/OpenAI 类认证回调，`54545`=Claude/Anthropic 认证回调，`11451`=iflow 认证回调，`51121`=antigravity 认证回调；这些多为上游默认保留的 OAuth callback 端口。
- 用户新增偏好：以后通过 `q2a` 处理图片时，默认按“真正图片消息/附件”发送，不要只发裸链接；视频也优先按真正视频发送，只有附件路径不通时才退回链接。
