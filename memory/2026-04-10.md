## 2026-04-10
- 用户要求先暂停对“小弟”（opencode）WebUI provider 下拉显示问题的继续排查；当前结论是 WebUI 实际吃 `/global/config`，且只暴露 `openai` provider，`cpap/r2/r3` 未进入前端配置。
- 用户随后要求先在 OpenClaw 自身配置里加入 `99dun` provider，并配置两个模型：`gpt-5.4`、`gpt-5.3-codex`。
- 用户同意并要求执行 gateway 重启以使 99dun 在 OpenClaw 正式生效。

- 2026-04-10 pre-compaction flush：继续排查“小弟”(opencode) WebUI provider 显示问题时，确认 `/usr/lib/node_modules/opencode-ai/bin/.opencode` 暴露关键环境变量 `OPENCODE_CONFIG`、`OPENCODE_CONFIG_CONTENT`、`OPENCODE_STRICT_CONFIG_DEPS`。
- 已为 `opencode-web` 增加 systemd drop-in：`/etc/systemd/system/opencode-web.service.d/override.conf`，注入 `Environment=OPENCODE_CONFIG=/root/.config/opencode/config.json` 与 `Environment=OPENCODE_STRICT_CONFIG_DEPS=1`，并重启服务；确认环境变量会影响服务行为，说明可通过环境变量干预 opencode 配置来源。
- 进一步验证时，公网抓取 `https://oc.fisht.de5.net/global/config` 命中 `401 Unauthorized`，而 `systemctl status opencode-web` 仍为 active/running，说明当前阻碍是认证/会话层，而非服务挂掉。
- 后续又确认运行中的 opencode 进程环境里真实存在完整的 `OPENCODE_CONFIG_CONTENT=...`，其中包含 `dun99 / cpap / r2 / r3`；结论：配置输入已进进程，但 WebUI 输出层仍未把 custom providers 正常暴露给前端，问题更聚焦在认证层或服务端 `/global/config`/`/provider` 的归一化输出逻辑。
- 下一步排查方向已明确：先绕过/理清 `Unauthorized`，恢复稳定抓取前端真实请求，再继续盯 `/global/config` 与 `/provider`，确认 custom provider 是被会话层挡住还是被服务端折叠成 `openai`。

- 2026-04-10 pre-compaction flush：排查“小弟”WebUI provider 问题时取得关键反转结论——使用**正确的内层后端认证**访问 opencode 原始接口后，服务端并未把 custom providers 折叠成 `openai`。
- 已确认后端正确认证方式为：`Authorization: Basic base64("opencode:<OPENCODE_SERVER_PASSWORD>")`。
- 在该认证条件下，后端真实返回为：
  - `GET /global/config` → `model = dun99/gpt-5.3-codex`，`small_model = dun99/gpt-5.3-codex-spark`，`provider_keys = ['dun99','cpap','r2','r3']`
  - `GET /provider` → 返回 `dun99 / cpap / r2 / r3` 四个 provider，且 `source=config`
- 因此问题定义已更新：根因不再是“服务端 /global/config 把 custom providers 折叠成 openai”，而更可能是**前端实际会话/认证流不对**，或**模型选择器存在额外过滤逻辑**，导致 UI 没展示真实 provider 数据。
- 为继续网页端验收，新建浏览器自动化脚本：`/root/.openclaw/workspace/oc_trace_ui_live.js`，用于同时携带：
  - 外层 Nginx Basic Auth（`ocadmin / T5w!2nQp9Kx7mV4`）
  - 内层后端 Basic Auth（`opencode:<OPENCODE_SERVER_PASSWORD>`）
  并抓取页面加载后与 `/provider`、`/global/config`、workspace/model 相关请求及可见 DOM，目标是定位“前端没拿到数据”还是“拿到了又被 UI 过滤掉”。
- 用户新增要求：后续应建立一个“监督+验收”子agent，负责定时确认进展与做网页端验收，验收不通过则继续推动修复。

- 2026-04-10 pre-compaction flush：用户要求建立“监督+验收”子agent：每 3 分钟确认进展，无进展则督促换打法；并负责对“小弟”网页端做验收，验收不通过则继续推动修复。
- 已尝试拉起独立监督子会话，但当前机器工具层存在阻塞：
  - `subagent` 创建时报参数兼容错误：`streamTo is only supported for runtime=acp; got runtime=subagent`
  - 改走 ACP 持久会话后又被本机 gateway pairing 阻塞：`pairing required`
- 因此暂未成功建立真正独立的监督子agent；已改为在主会话内执行“监督模式”：每轮推进都要给出证据，若无实质进展则强制切换为本质不同的排查方法，直到网页端验收通过。
- 已与用户明确当前验收标准不缩水：
  1. 真进入 workspace
  2. provider/model 可选
  3. 可明确区分 `dun99 / cpap / r2 / r3`
  4. 至少一次实际对话回包
  5. 任一未达成则继续修，不算完成
- 当前核心结论保持：服务端真实接口在正确认证下已返回 `dun99 / cpap / r2 / r3`，问题已收缩到前端页面流 / 认证流 / 模型选择器过滤逻辑，而非 provider 配置格式本身。

- 2026-04-10 pre-compaction flush：已向用户同步当前进展——通过重新打通“小弟”后端真实接口，确认 `/global/config` 与 `/provider` 在正确认证下都返回 `dun99 / cpap / r2 / r3`，因此进一步排除“服务端把 custom provider 折叠成 openai”这一根因。
- 当前问题已收缩到网页端真实页面流：前端认证/会话、进入 workspace 后的模型选择器，以及其数据获取/过滤逻辑。
- 最近一次网页端自动化实测脚本运行失败，关键报错为：`page.waitForTimeout: Target page, context or browser has been closed`。
- 对用户的最新状态汇报结论：后端层基本已洗清，当前主战场是 WebUI 前端行为本身；后续将优先修稳浏览器自动化脚本，重新抓取进入 workspace 后模型选择器的实际请求与页面渲染数据，直到完成网页端验收（provider 可选、可区分、可对话回包）。

- 2026-04-10 pre-compaction flush：用户再次要求建立独立“监督+验收”子agent，用于每 3 分钟确认进展、无进展则督促换打法，并负责网页端验收，不通过则继续推动修复。
- 已再次尝试拉起独立子agent，但当前 OpenClaw 的 agent spawn 通道仍受工具层阻塞：
  - `subagent` 路径报错：`streamTo is only supported for runtime=acp; got runtime=subagent`
  - `acp` 路径此前报错：`pairing required`
  - 当前可见会话中没有现成可复用的监督子会话
- 已向用户明确：独立监督子agent 当前不是“不想拉”，而是机器当前创建通道本身有兼容/配对阻塞；因此暂时继续采用“主会话监督模式”作为等效替代：每轮必须汇报证据，若无实质进展则强制切换为本质不同的排查方法，直到网页端验收通过。
- 当前主线技术结论未变：`/global/config` 与 `/provider` 在正确认证下都返回 `dun99 / cpap / r2 / r3`，问题仍聚焦在 WebUI 前端页面流 / 模型选择器逻辑，而非服务端 provider 配置格式。

- 2026-04-10 pre-compaction flush：继续排查“小弟”网页端验收时，最小化 Playwright 实测得到新硬证据：
  - 页面标题为 `401 Authorization Required`
  - 页面正文为 `nginx/1.18.0 (Ubuntu)`
  - 控制台报错 `Failed to load resource: the server responded with a status of 401 ()`
- 结论：当前网页端自动化链路**仍未稳定穿过外层 Nginx Basic Auth**，因此无法进入 workspace 或验证模型选择器。
- 问题已进一步收敛：先解决浏览器自动化对外层 Nginx Basic Auth 的稳定通过，再考虑内层 opencode 认证与 workspace/model selector 验收。
- 新建最小化排障脚本：`/root/.openclaw/workspace/oc_min_probe.js`，用于验证页面是否自行关闭/跳转及 401 行为。
